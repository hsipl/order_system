import 'package:flutter/material.dart';

class DeletedOrderPage extends StatelessWidget {
  const DeletedOrderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text('DeletedOrderPage'),
    ));
  }
}
