import 'package:flutter/material.dart';

class BlockGoodsPage extends StatelessWidget {
  const BlockGoodsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text('BlockGoodsPage'),
    ));
  }
}
