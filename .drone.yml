kind: pipeline
name: drone-CICD

steps:
- name: unit-test
  image: python
  commands:
  - echo "ok!" 

- name: ssh commands
  image: appleboy/drone-ssh
  settings:
    host: 
      from_secret: host 
    username: 
      from_secret: user 
    password: 
      from_secret: password 
    port: 22
    script:
      - cd /home/hsipl/feng/Order_System/prd/
      - git pull https://github.com/Milesfeng/order_system.git

trigger:
  branch: 
    - main
  event: 
    - pull_request